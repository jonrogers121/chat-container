import styled from "styled-components";
import { px2Rem } from "../../utils/px2Rem";

const StyledMessageList = styled.div``;

const Spacer = styled.div`
  margin-bottom: ${px2Rem(24)};
`;

export { StyledMessageList, Spacer };
